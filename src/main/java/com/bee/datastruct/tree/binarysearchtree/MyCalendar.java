package com.bee.datastruct.tree.binarysearchtree;import java.util.Map;import java.util.TreeMap;/** * 剑指 Offer II 058. 日程表：https://leetcode.cn/problems/fi9suh/ * * @author yangying * @version 1.0 * @since 2022/5/28 **/public class MyCalendar {    TreeMap<Integer,Integer> map;    public MyCalendar() {        map = new TreeMap<>();//Treemap 保持日程安排，key是起始时间，value是结束时间    }    public boolean book(int start, int end) {          Map.Entry<Integer,Integer> entryStart = map.floorEntry(start);//比start早的最近一个日程          if(entryStart!=null && entryStart.getValue()>start){//已安排的日程结束时间不能大于start              return false;          }        Map.Entry<Integer,Integer> entryEnd = map.ceilingEntry(start);//比start晚的最近一个日程        if(entryEnd!=null && entryEnd.getKey()<end){//已安排的日程开始时间不能早于end            return false;        }        map.put(start,end);        return true;    }}